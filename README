dmenu - dynamic menu
====================
dmenu is an efficient dynamic menu for X.


Requirements
------------
In order to build dmenu you need the Xlib header files.


Installation
------------
Edit config.mk to match your local setup (dmenu is installed into
the /usr/local namespace by default).

Afterwards enter the following command to build and install dmenu
(if necessary as root):

    make clean install


Running dmenu
-------------
See the man page for details.

Patches
-------

The patches applied to this branch are:

    - Border
    - Fuzzy Match
    - Fuzzy Highlight
    - Line Height
    - Center
    - Numbers

Launch Options
--------------

This launch options use the dracula theme.

dmenu_run -c -h 24 -fn "Fira Code" -l 10 -nb "#44475a" -nf "#f8f8f2" -sb "#bd93f9" -sf "#282a36" -shb "#bd93f9" -shf "#8be9fd" -nhb "#44475a" -nhf "#8be9fd"

This laucnh with gruvbox theme like.
dmenu_run -c -h 24 -fn "Fira Code" -l 10 -nb "#1d2021" -nf "#8e9090" -sb "#928374" -sf "#49413a" -shb "#928374" -shf "#ffffff" -nhb "#1d2021" -nhf "#ffffff"
